from __future__ import annotations

from app import legacy

normalize_role = legacy.normalize_role
is_privileged_role = legacy.is_privileged_role
current_user_role = legacy.current_user_role
get_buttons = legacy.get_buttons
buttons_for_role = legacy.buttons_for_role
allowed_preconfigured_commands = legacy.allowed_preconfigured_commands
